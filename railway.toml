[build]
buildCommand = "cd scripts && pip install -r requirements.txt"

[deploy]
startCommand = "cd scripts && gunicorn auth_api:app --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
